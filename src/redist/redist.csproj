<Project Sdk="Microsoft.NET.Sdk" ToolsVersion="15.0">
  <PropertyGroup>
    <TargetFramework>netcoreapp3.0</TargetFramework>
    <GenerateRuntimeConfigurationFiles>true</GenerateRuntimeConfigurationFiles>
    <CopyBuildOutputToPublishDirectory>false</CopyBuildOutputToPublishDirectory>
    <DisableImplicitFrameworkReferences>false</DisableImplicitFrameworkReferences>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\dotnet-core-uninstall\dotnet-core-uninstall.csproj" />
  </ItemGroup>

  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\Versions.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\BuildCoreSdkTasks.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\GetRuntimeInformation.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\GenerateLayout.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\FileExtensions.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\GenerateMSIs.targets" />
  <Import Condition="'$(RuntimeIdentifier)' == 'win-x86'" Project="targets\Signing.targets" />

  <Target Condition="'$(RuntimeIdentifier)' == 'win-x86'" Name="GenerateMSIs" AfterTargets="Build" DependsOnTargets="GenerateLayout;GenerateDotnetCoreUninstallMsi">
  </Target>

</Project>
